[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.28"
evm_version = "cancun"
optimizer = true
optimizer_runs = 800
via_ir = true
# No first-party test/script uses `vm.ffi`; keep it off so a dirty checkout can't run host commands
# (security-audit I-03). Re-enable in an explicit profile only if a future script needs it.
ffi = false
fs_permissions = [{ access = "read-write", path = ".forge-snapshots/" }]
# v4 flash accounting relies on transient storage (EIP-1153 / cancun) and large gas limits.
gas_limit = "3000000000"

[profile.default.fuzz]
runs = 256

# `via_ir` is enabled by default (~1.2% lower runtime gas, measured on the fork suite; also avoids
# "stack too deep" in the heavier periphery). This `viair` profile is kept as an explicit alias.
[profile.viair]
via_ir = true
optimizer_runs = 800

[rpc_endpoints]
mainnet = "${ETH_RPC_URL}"

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
